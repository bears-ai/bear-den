{
  "name": "Workspace",
  "dockerComposeFile": "../docker-compose.yaml",
  "service": "workspace",
  "workspaceFolder": "/workspace",
  "postStartCommand": "docker compose --profile bundled up -d bear-postgres bears-memfs-manager bears-den bears-pool"
}
