# Diagnostics

macOS-specific diagnostics and support helpers.

Phase-0 should at least support:

- reporting installed adapter path;
- reporting bundled and installed adapter versions;
- exposing last install/update result;
- assembling a copyable diagnostic summary.
