# Platform

macOS-specific platform helpers for the Bears app.

Likely responsibilities:

- application support path resolution;
- bundle resource inspection;
- Finder reveal actions;
- clipboard integration;
- other macOS-only utility code that should not live in shared AppCore.
