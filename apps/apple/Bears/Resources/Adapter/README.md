# Adapter bundle resources

Place the signed or development `bears-acp-adapter` executable here for local app builds.

Expected resource path for the current SwiftPM app scaffold:

- `Resources/Adapter/bears-acp-adapter`

This file is a placeholder so the resource directory is visible in the repo before the actual adapter bundling step is automated.
