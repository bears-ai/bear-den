// Transitional compatibility re-exports while phase-0 naming is cleaned up.
pub use crate::core::runtime_contracts::{
    acp_requires_letta_runtime, InteractionRunStore, RetrievalService, RoleProfileRegistry,
    RoleRunner, RuntimeHealthCheck, RuntimeStartupCapabilities, ToolActuatorRegistry,
};
